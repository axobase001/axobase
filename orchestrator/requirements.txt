# FeralLobster Orchestrator 依赖
# Python 3.11+

# Web 框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# 数据库
sqlalchemy>=2.0.0
alembic>=1.13.0

# 区块链
web3>=6.15.0
eth-abi>=5.0.0

# 存储
arweave-python-client>=1.0.0
# 或 bundlr-network/client (需要 Node.js)

# 哈希计算
blake3>=0.3.0

# 模板
jinja2>=3.1.0

# 异步任务 (可选)
celery>=5.3.0
redis>=5.0.0

# 环境变量
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# 数据库驱动
# PostgreSQL: psycopg2-binary>=2.9.0
# SQLite: 内置

# HTTP 客户端
aiohttp>=3.9.0
httpx>=0.26.0

# 工具
pydantic>=2.5.0
email-validator>=2.1.0

# 开发
pytest>=7.4.0
pytest-asyncio>=0.23.0
black>=23.0.0
